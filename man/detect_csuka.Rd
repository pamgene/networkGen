% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-prep.R
\name{detect_csuka}
\alias{detect_csuka}
\title{Detect whether a raw UKA export is a csUKA (per-comparison) export}
\usage{
detect_csuka(uka)
}
\arguments{
\item{uka}{A raw UKA data frame (Tercen-style dotted column names are
fine -- only the last dot-segment is inspected, as \code{\link[=clean_tercen_columns]{clean_tercen_columns()}}
would produce).}
}
\value{
\code{TRUE} if the columns look like a csUKA export (a bare
\verb{Specificity Score} present and no \verb{Mean Specificity Score}), \code{FALSE}
otherwise -- including when neither is present, so a malformed input
fails later at the column-selection step with a clearer message rather
than here.
}
\description{
A regular UKA export aggregates its metric columns across comparisons and
names them accordingly -- \verb{Mean Specificity Score}, \verb{Median Kinase Statistic}, \verb{Mean Significance Score}, \verb{Median Final score}. A csUKA
(per-comparison) export carries one value per comparison and names the
same columns without the \code{Mean}/\code{Median} prefix -- \verb{Specificity Score},
\verb{Kinase Statistic}, etc. The \verb{clean_uka_to_kinograte*()} functions read
different columns for each; this picks which based on what's present, so
callers don't have to pass a \code{cs} flag by hand.
}
