% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pcsf-core.R
\name{kinograte_pg_pcsf}
\alias{kinograte_pg_pcsf}
\title{Build a PCSF network from prize data and a PPI network}
\usage{
kinograte_pg_pcsf(
  df,
  ppi_network,
  maintitle,
  n = 8,
  w = 10,
  r = 0.1,
  b = 1.5,
  mu = 0.005,
  cluster = TRUE,
  seed = NULL,
  res.path,
  spec_cutoff,
  condition,
  write
)
}
\arguments{
\item{df}{Data frame of terminal nodes with columns \code{name}, \code{prize}
(and typically \code{type}, \code{LogFC}), e.g. from \code{\link[=uka_top]{uka_top()}} / \code{\link[=sens_top]{sens_top()}}.}

\item{ppi_network}{Data frame with columns \code{head}, \code{tail}, \code{cost} -- the
PPI network to search over. Always required explicitly; never defaults
to a global.}

\item{maintitle}{Ignored if given; recomputed from \code{condition}/\code{spec_cutoff}.}

\item{n, w, r, b, mu}{Passed to \code{\link[=PCSF_rand_pg]{PCSF_rand_pg()}}.}

\item{cluster}{If \code{TRUE} (default), cluster the resulting network with
\code{igraph::edge.betweenness.community()} and include \code{wc_df}.}

\item{seed}{Optional RNG seed for reproducibility.}

\item{res.path}{Output folder, used only when \code{write = TRUE}.}

\item{spec_cutoff}{Specificity cutoff, recorded in \code{maintitle} and (when
\code{write = TRUE}) output file names.}

\item{condition}{Condition/comparison label, recorded in \code{maintitle} and
(when \code{write = TRUE}) output file names.}

\item{write}{If \code{TRUE}, write \verb{nodes_<condition>_spec<spec_cutoff>.csv},
\verb{edges_<condition>_spec<spec_cutoff>.csv}, \verb{wc_df_<condition>_spec<spec_cutoff>.csv}
(if \code{cluster = TRUE}), and (if any) \verb{missing_nodes_<condition>_spec<spec_cutoff>.csv}
to \code{res.path}. Pass \code{\link[=prepare_run_params]{prepare_run_params()}}'s resulting \code{respath} as
\code{res.path} to get a parameter-encoded, provenance-tracked folder rather
than an arbitrary caller-chosen one.}
}
\value{
A list: \code{network} (igraph), \code{nodes}, \code{edges}, \code{missing}/\code{missing_nodes}
(data frames), \code{wc_df} (if \code{cluster = TRUE}), \code{maintitle}. \code{NULL} if PCSF
fails to find a subnetwork.
}
\description{
Calls \code{\link[=PCSF_rand_pg]{PCSF_rand_pg()}} directly on \code{ppi_network} without going through
\code{PCSF::construct_interactome()}. Used by \code{\link[=generate_paired_network]{generate_paired_network()}} /
\code{\link[=generate_kinase_network]{generate_kinase_network()}}.
}
