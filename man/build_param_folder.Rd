% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params.R
\name{build_param_folder}
\alias{build_param_folder}
\title{Build a parameter-encoded folder name for a networkGen run}
\usage{
build_param_folder(params)
}
\arguments{
\item{params}{A parameters list from \code{\link[=capture_params]{capture_params()}} (must carry the
\code{"symbol_names"} attribute it sets). Recognized names: \code{uka}, \code{sens}
(optional -- nests the result under a \code{uka}-named subfolder when
present, for paired analyses), \code{ppi_network}, \code{spec_cutoff} (single
value or vector; omitted from the name entirely when \code{0}, since \code{0}
means "no filtering on this cutoff"), \code{perc_cutoff} (same omit-if-0
rule), \code{sens_perc_cutoff} (optional -- the sensitivity-side percentile
cutoff, gridded independently of \code{perc_cutoff}; same omit-if-0 rule),
\code{rank_uka_abs}, \code{b}, \code{w}, \code{cs} (optional), \code{art_nodes} (optional).}
}
\value{
The folder name (a single path segment, or \code{uka_name/components}
when \code{sens} is present), as a string.
}
\description{
The hardcoded naming rule: reads a fixed set of recognized parameter
names out of \code{params} (as returned by \code{\link[=capture_params]{capture_params()}}) and encodes
them into one folder-name string, one folder per unique parameter
combination -- e.g. 4 conditions x 2 \code{spec_cutoff} values is 8 folders,
not one folder covering a sweep of cutoffs. Pure function: no filesystem
side effects, no dependence on \code{respath} (joining onto a base path and
creating the directory is \code{\link[=prepare_run_params]{prepare_run_params()}}'s job).
}
\details{
Only understands \code{networkGen}-native parameters. \code{networkScore} composes
its own folder-naming on top of this rather than this function knowing
about scoring-only parameters like \code{nperms_network} -- see
\code{networkScore::build_score_param_folder()}.
}
