% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/percentile-score.R
\name{percentile_score_fast}
\alias{percentile_score_fast}
\title{Percentile-rank a data frame by a metric (data.table-backed, fast path)}
\usage{
percentile_score_fast(df, symbol, metric, desc = FALSE)
}
\arguments{
\item{df}{Data frame with the columns named by \code{symbol} and \code{metric}.}

\item{symbol}{Column (unquoted) with the row identifier.}

\item{metric}{Column (unquoted) to rank by.}

\item{desc}{If \code{TRUE}, rank descending by \code{-abs(metric)} instead of \code{abs(metric)}.}
}
\value{
A \code{data.table}, deduplicated by \code{symbol} (renamed to \code{name}), with
a \code{percentile_score} column, sorted by that column descending.
}
\description{
Same semantics as \code{\link[=percentile_score]{percentile_score()}} (ranks by \code{abs(metric)}), but
implemented with \code{data.table} for speed on large inputs (e.g. permutation
loops in networkScore, called once per permutation).
}
