% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grid.R
\name{normalize_ppi_network_list}
\alias{normalize_ppi_network_list}
\title{Normalize \code{ppi_network} to a named list of one or more reference networks}
\usage{
normalize_ppi_network_list(ppi_network, single_label)
}
\arguments{
\item{ppi_network}{A data frame, or a fully named list of data frames.}

\item{single_label}{Label to use as the list name when \code{ppi_network} is a
single data frame -- the caller's own correctly-captured expression
(see \code{\link[=run_network_grid]{run_network_grid()}}/\code{\link[=build_network_grid]{build_network_grid()}} for why this can't
just be derived here with \code{rlang::enquo()}).}
}
\value{
A fully named list of data frames.
}
\description{
\code{ppi_network} is either a single data frame (the common case -- wrapped
into a length-1 named list using \code{single_label}) or an explicitly, fully
named list of them (for gridding across more than one reference network).
Exported so \code{networkScore}'s scoring entry points (which grid \code{ppi_network}
the same way) can reuse it rather than duplicating this normalization.
}
