% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grid.R
\name{build_network_grid}
\alias{build_network_grid}
\title{Expand a parameter grid into a flat list of per-network build inputs}
\usage{
build_network_grid(
  raw_uka,
  clean_fn = identity,
  condition_col,
  spec_cutoff,
  perc_cutoff,
  b,
  rank_uka_abs = TRUE,
  ppi_network = ppi_networkv12,
  dataset_col = "dataset"
)
}
\arguments{
\item{raw_uka}{Raw kinase-activity data. If it has a \code{dataset_col} column
(e.g. from \code{\link[=load_uka_dataset_files]{load_uka_dataset_files()}}), it's split by that column
before cleaning -- each dataset cleaned and gridded independently. If
not, treated as a single implicit dataset.}

\item{clean_fn}{Function \code{(raw_uka_subset) -> cleaned} producing a data
frame with \code{fscore}/\code{uniprotname}/\code{LogFC} and \code{condition_col} columns
(e.g. \code{networkGen::clean_uka_to_kinograte()}, \code{networkScore}'s
\code{clean_uka_to_kinograte_kinase()}/\code{clean_uka_to_kinograte_full()} --
whichever convention the caller's condition-identifying column uses).
Default \code{identity}: \code{raw_uka} is already cleaned.}

\item{condition_col}{Name of the column identifying each condition in
\code{clean_fn}'s output (e.g. \code{"Sgroup_contrast"}, \code{"Sample"}, \code{"cell_line"}
-- differs by which cleaning convention \code{clean_fn} uses).}

\item{spec_cutoff, perc_cutoff, b, rank_uka_abs}{Vectors -- every combination
is gridded, not just paired elementwise. \code{spec_cutoff}/\code{perc_cutoff}
are passed to \code{\link[=uka_top]{uka_top()}} (which also determines \code{uka_filt});
\code{rank_uka_abs} too. \code{b} doesn't affect filtering, only the later PCSF
build, but is gridded here alongside the others for one uniform
mechanism rather than a second, separate expansion step.}

\item{ppi_network}{A data frame with columns \code{head}, \code{tail}, \code{cost}, or a
fully named list of them (e.g. \code{list(v12 = ppi_networkv12, kins502 = ppi_networkv12_502_kins)}) to grid across more than one reference
network. Defaults to the bundled \link{ppi_networkv12}.}

\item{dataset_col}{Name of the dataset-identifying column, if present in
\code{raw_uka}. Default \code{"dataset"}.}
}
\value{
A list, one element per \verb{(dataset, condition, spec_cutoff, perc_cutoff, b, rank_uka_abs, ppi_network)} combination, each a list
with \code{dataset}, \code{condition}, \code{spec_cutoff}, \code{perc_cutoff}, \code{b},
\code{rank_uka_abs}, \code{ppi_network_name}, \code{ppi_network} (the actual data
frame for that name), \code{uka_filt} (top-hit terminal-node data frame,
from \code{\link[=uka_top]{uka_top()}}), \code{uka_cell_all} (that condition's full, unfiltered
rows -- for permutation building).
}
\description{
The one place \verb{dataset x condition x spec_cutoff x perc_cutoff x b x rank_uka_abs x ppi_network} gets expanded -- both \code{\link[=run_network_grid]{run_network_grid()}}
(plain generation) and \code{networkScore}'s scoring entry points build on
this, so there is exactly one grid-construction mechanism in the whole
suite (see \code{docs/adr/0006-unified-grid-interface.md}). Pure: no
filesystem access, no network builds -- just data in, a flat list of
resolved cells out. Every one of \code{spec_cutoff}, \code{perc_cutoff}, \code{b},
\code{rank_uka_abs}, \code{ppi_network} can be a vector (or, for \code{ppi_network}, a
named list) -- every combination is gridded, not just the ones a
particular caller happens to demonstrate.
}
