% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params.R
\name{capture_params}
\alias{capture_params}
\title{Capture the parameters used for a run}
\usage{
capture_params(..., .labels = list())
}
\arguments{
\item{...}{Named (or unnamed, using the argument's own variable name)
parameters to capture.}

\item{.labels}{Named list/character vector overriding specific
parameters' labels, instead of deriving them from the expression
written at this call site. For when the immediate caller is itself a
relay -- e.g. \code{run_network_grid()} re-binding its own \code{raw_uka}
argument to \code{uka} before calling this -- the expression visible \emph{here}
would just be the relay's own local variable name (\code{raw_uka}), not
the original caller's (\code{cleaned_batch}). A relay should capture its own
caller's expression with \code{rlang::as_label(rlang::enquo(raw_uka))}
\emph{before} re-binding, and pass it through here.}
}
\value{
The evaluated parameters as a named list, with a \code{"symbol_names"}
attribute recording each parameter's label as text.
}
\description{
Generic capture only: evaluates \code{...} and records each parameter's value
plus a human-readable label for it, so a caller can recover \emph{what} was
used without re-deriving it from context. Has no knowledge of what any
particular parameter name means -- see \code{\link[=build_param_folder]{build_param_folder()}} for the
hardcoded rule that turns a captured \code{uka}/\code{ppi_network}/\code{spec_cutoff}/...
shaped \code{params} into a folder name, and \code{\link[=prepare_run_params]{prepare_run_params()}} for the
two composed together plus the filesystem side effects (folder creation,
writing \code{params.csv}).
}
\details{
For a scalar (length-1 atomic) parameter, the label is the evaluated
\emph{value} -- so \code{capture_params(spec_cutoff = sc)} inside a loop over \code{sc}
records \code{0.7}, not the loop variable's name \code{sc}. For anything else (a
data frame, a vector), the label is the call-site expression text (e.g.
the variable name \code{uka}) instead, since dumping a data frame's full
content wouldn't be readable.
}
