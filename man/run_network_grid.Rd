% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grid.R
\name{run_network_grid}
\alias{run_network_grid}
\title{Build every network in a parameter grid, in one flattened batch}
\usage{
run_network_grid(
  raw_uka,
  clean_fn = identity,
  comparison_col,
  spec_cutoff,
  perc_cutoff,
  b = 2,
  w = 2,
  ppi_network = ppi_networkv12,
  rank_uka_abs = TRUE,
  dataset_col = "dataset",
  sens = NULL,
  sens_perc_cutoff = NULL,
  sens_balance = TRUE,
  respath,
  write = TRUE,
  max_tasks = 500,
  ...
)
}
\arguments{
\item{raw_uka}{Raw kinase-activity data. If it has a \code{dataset_col} column
(e.g. from \code{\link[=load_uka_dataset_files]{load_uka_dataset_files()}}), it's split by that column
before cleaning -- each dataset cleaned and gridded independently. If
not, treated as a single implicit dataset.}

\item{clean_fn}{Function \code{(raw_uka_subset) -> cleaned} producing a data
frame with \code{fscore}/\code{uniprotname}/\code{LogFC} and \code{comparison_col} columns
(e.g. \code{\link[=prep_uka]{prep_uka()}}, or \code{networkScore::prep_uka_paired()} -- whichever
convention the caller's comparison-identifying column uses). Default
\code{identity}: \code{raw_uka} is already reshaped.}

\item{comparison_col}{Name of the column identifying each comparison in
\code{clean_fn}'s output (e.g. \code{"Sgroup_contrast"}, \code{"Sample"}, \code{"cell_line"}
-- differs by which reshaping convention \code{clean_fn} uses).}

\item{spec_cutoff, perc_cutoff, b, w, rank_uka_abs}{Vectors -- every
combination is gridded, not just paired elementwise. \code{spec_cutoff}/
\code{perc_cutoff} are passed to \code{\link[=uka_top]{uka_top()}} (which also determines
\code{uka_filt}); \code{rank_uka_abs} too. \code{b} and \code{w} don't affect filtering,
only the later PCSF build (see \code{\link[=generate_kinase_network]{generate_kinase_network()}} for what
they do), but are gridded here alongside the others for one uniform
mechanism rather than a second, separate expansion step. \code{b}/\code{w}
default to \code{2} (see \code{\link[=PCSF_rand_pg]{PCSF_rand_pg()}}).}

\item{ppi_network}{A data frame with columns \code{head}, \code{tail}, \code{cost}, or a
fully named list of them (e.g. \code{list(v12 = ppi_networkv12, kins502 = ppi_networkv12_502_kins)}) to grid across more than one reference
network. Defaults to the bundled \link{ppi_networkv12}.}

\item{dataset_col}{Name of the dataset-identifying column, if present in
\code{raw_uka}. Default \code{"dataset"}.}

\item{sens}{Optional raw sensitivity data frame (columns \code{uniprotname},
\code{LogFC} -- the shape \code{\link[=sens_top]{sens_top()}} expects); if given, builds paired
networks for every cell. If \code{NULL} (default), builds kinase-only
networks. One sensitivity profile is shared across every condition
(unlike \code{uka}, sensitivity here isn't per-condition) -- filtered
per \code{sens_perc_cutoff} value, not per condition.}

\item{sens_perc_cutoff}{Required when \code{sens} is given. A vector -- every
value is gridded (crossed against every other dimension) via
\code{\link[=sens_top]{sens_top()}}, the same way \code{perc_cutoff} grids \code{uka}. There is no
sensitivity-side equivalent of \code{spec_cutoff}: specificity is a
kinase-activity-only concept.}

\item{sens_balance}{Passed to \code{\link[=sens_top]{sens_top()}} as \code{balance} for every
\code{sens_perc_cutoff} value -- fixed, not gridded. Default \code{TRUE}.}

\item{respath}{Base output directory for \code{write = TRUE}.}

\item{write}{If \code{TRUE} (default), write each build's \code{nodes}/\code{edges}/
\code{wc_df} CSVs via \code{\link[=prepare_run_params]{prepare_run_params()}}-tracked folders.}

\item{max_tasks}{Refuse to proceed (\code{stop()}, without building anything)
if the grid expands to more than this many networks -- a safety guard
against an unintentionally huge overnight run, not a time estimate.
Raise explicitly if the size is actually intended. Default 500.}

\item{...}{Additional arguments passed to every build, uniform across
the whole grid (not gridded) -- e.g. \code{n}, \code{w}, \code{r}, \code{mu}, \code{seed}.}
}
\value{
Same shape as \code{\link[=generate_networks_batch]{generate_networks_batch()}}: a list, one element
per grid cell, each with \code{result} and \code{meta} (\code{dataset}, \code{condition},
\code{spec_cutoff}, \code{perc_cutoff}, \code{b}, \code{w}, \code{rank_uka_abs},
\code{ppi_network_name}, and, for the paired path, \code{sens_perc_cutoff}).
}
\description{
The unified entry point for plain (non-scoring) generation across a
\verb{dataset x condition x spec_cutoff x perc_cutoff x b x w x rank_uka_abs x ppi_network} grid -- the caller specifies the grid, not loops. Replaces
\code{generate_networks_for_conditions()} (removed -- see
\code{docs/adr/0006-unified-grid-interface.md}): the single-file,
single-combination case is just this function called with length-1
vectors, so keeping a separate "simple" function only duplicated logic
for no real gain.
}
\details{
One \code{\link[=prepare_run_params]{prepare_run_params()}} folder is created per distinct combination
when \code{write = TRUE} -- every dataset/condition sharing that combination
writes into the same folder, distinguished by file name, not one folder
per cell. All cells across the whole grid are still submitted as a
single \code{\link[=generate_networks_batch]{generate_networks_batch()}} call (full flattening, see
\code{docs/adr/0003-full-flattening-parallelization.md}).
}
