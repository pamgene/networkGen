% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{generate_networks_batch}
\alias{generate_networks_batch}
\title{Build many independent networks in parallel}
\usage{
generate_networks_batch(
  tasks,
  generate_fn,
  ppi_network = NULL,
  extra_args = list(),
  progress = FALSE
)
}
\arguments{
\item{tasks}{A list of tasks, each a list with:
\itemize{
\item \code{args}: named list of arguments to pass to \code{generate_fn} for this
build (e.g. \code{uka}, \code{sens}, \code{spec_cutoff}, \code{b}, \code{condition}).
\item \code{meta}: arbitrary data to carry through untouched, for the caller's
own bookkeeping (e.g. \code{list(condition = "A_vs_B", role = "permutation", perm_index = 3)}).
}}

\item{generate_fn}{The single-network build function to call for every
task -- \code{\link[=generate_paired_network]{generate_paired_network()}} or \code{\link[=generate_kinase_network]{generate_kinase_network()}}.}

\item{ppi_network}{Data frame with columns \code{head}, \code{tail}, \code{cost} -- the
PPI network used for any task that doesn't specify its own via
\code{task$args$ppi_network} (the common case: one network shared across
the whole batch). A task carrying its own \code{ppi_network} -- e.g. when
gridding across more than one reference network -- overrides this.}

\item{extra_args}{Named list of additional arguments merged into every
task's \code{args} (task-specific values in \code{args} take precedence over
same-named entries here). Typically constant across the whole batch,
e.g. \code{list(write = FALSE)}.}

\item{progress}{If \code{TRUE}, show a \code{furrr} progress bar.}
}
\value{
A list, same length and order as \code{tasks}, each element a list of
\code{result} (the \code{"networkGen_result"} from \code{generate_fn}, or \code{NULL} if
that build failed) and \code{meta} (unchanged from the input task).
}
\description{
The single parallelization primitive for this whole network suite (see
\code{docs/adr/0003-full-flattening-parallelization.md} -- that decision is
substantively about \code{networkScore}'s design, filed here since suite-wide
ADRs are anchored in this package's repo).
Has no concept of what the build requests represent -- whether they're
many real conditions (plain batch generation) or a permutation task list
(\code{networkScore}'s observed + permuted builds, flattened across every
condition being scored) is entirely up to the caller. Each task carries
its own \code{meta}, returned untouched alongside its result, so the caller can
regroup results afterward without this function needing to understand the
tags.
}
\details{
Runs against whatever \code{future::plan()} is currently active in the calling
session -- set one (e.g. \code{future::plan(future::multisession, workers = 4)})
before calling this for actual parallelism; the default \code{sequential} plan
runs the batch one build at a time. Set up the plan once per session and
reuse it across calls, rather than creating/tearing down a plan per batch
-- worker startup (including loading \code{PCSF}'s compiled code, see below)
has real cost.

\code{PCSF}'s compiled \code{.Call("_PCSF_call_sr", ...)} requires the \code{PCSF}
namespace to be loaded in each worker process -- a real requirement of
compiled/Rcpp-style packages under socket-cluster parallelism, not an
instance of global-environment coupling. This is declared explicitly via
\code{furrr_options(packages = "PCSF")} below rather than relying on \code{future}'s
automatic package re-attachment, since this exact failure mode
(\verb{"_PCSF_call_sr" not available for .Call()}) has been hit before in this
project's history.
}
