% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uka-files.R
\name{load_uka_dataset_files}
\alias{load_uka_dataset_files}
\title{Load and tag raw UKA files by dataset, merging PTK/STK pairs}
\usage{
load_uka_dataset_files(paths)
}
\arguments{
\item{paths}{Character vector of raw UKA CSV file paths.}
}
\value{
One combined data frame (raw Tercen-style columns, as read),
with \code{dataset} and \code{kinase_type} (\code{"PTK"}, \code{"STK"}, or \code{NA} for
non-matching files) columns added. Pass to \code{\link[=build_network_grid]{build_network_grid()}}.
}
\description{
The one place file-path/filename knowledge lives in this package --
everything downstream (\code{\link[=build_network_grid]{build_network_grid()}}, the cleaning functions)
stays filesystem-blind and works on plain data frames.
}
\details{
Real UKA exports are named \verb{UKA_<PTK|STK>_<number>_<dataset name>.csv}
(the dataset name itself may contain \verb{_}; it always comes after the
number and before \code{.csv}). A PTK file and an STK file are the same
dataset when they share both a folder and a \verb{<number>_<dataset name>}
suffix -- these get row-bound together, tagged \code{kinase_type} for
provenance. The folder is part of the dataset identity: two different
experiments can reuse the same \verb{<number>_<dataset name>} (e.g. both named
\verb{01_TvsC}), and are not the same dataset just because the suffix matches.

A path that doesn't match this naming convention falls back to using its
own file name (without extension) as a standalone dataset -- no PTK/STK
merging attempted, since there's nothing to merge.
}
