% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-prep.R
\name{prep_sens}
\alias{prep_sens}
\title{Reshape raw sensitivity data into network-builder input}
\usage{
prep_sens(sens, control, zscore = FALSE, del_cell = NULL)
}
\arguments{
\item{sens}{Raw sensitivity data frame with columns \code{TARGET_1}, \code{cell_line}, \code{LN_IC50}.}

\item{control}{If given, \code{LogFC} is computed as \code{LN_IC50 - LN_IC50} of
\code{control} for the same target (fold change vs. control). If \code{NULL} and
\code{zscore = FALSE}, \code{sens} is returned as-is (already-computed values, e.g.
median sensitivity). If \code{NULL} and \code{zscore = TRUE}, a per-target z-score
across cell lines is computed instead.}

\item{zscore}{See \code{control}.}

\item{del_cell}{Optional character vector of \code{cell_line} values to drop.}
}
\value{
Data frame with columns \code{cell_line}, \code{uniprotname}, \code{LogFC}.
}
\description{
Reshape raw sensitivity data into network-builder input
}
