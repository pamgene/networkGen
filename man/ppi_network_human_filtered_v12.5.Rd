% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{ppi_network_human_filtered_v12.5}
\alias{ppi_network_human_filtered_v12.5}
\title{Reference human protein-protein interaction network (STRING v12.5)}
\format{
A tibble with 1,218,528 rows and 3 columns:
\describe{
\item{head}{Gene symbol, interaction partner 1.}
\item{tail}{Gene symbol, interaction partner 2.}
\item{cost}{Edge cost for PCSF -- lower means a stronger/more confident interaction.}
}
}
\source{
STRING v12.5 (\url{https://string-db.org}), filtered as described
above; see the STRING publication (\url{https://academic.oup.com/nar/article/53/D1/D730/7903368})
for what each evidence channel means.
}
\usage{
ppi_network_human_filtered_v12.5
}
\description{
The PPI network used as \code{ppi_network} throughout this project's analyses
-- a filtered, high-confidence subset of STRING v12.5 human interactions.
Built by the STRING-download pipeline (see \code{DevOpti/STRING_download}'s
\code{generate_string_ppi.rmd} / \code{R/helper.R::generate_string_ppi()}), not
regenerated here; bundled as-is so \code{networkGen} ships a working reference
network out of the box instead of requiring every caller to independently
rebuild one. This is the default \code{ppi_network} for
\code{\link[=generate_paired_network]{generate_paired_network()}} and \code{\link[=generate_kinase_network]{generate_kinase_network()}}.
}
\details{
Built from STRING's "full" per-evidence-channel links file for
\emph{Homo sapiens} (taxon 9606): an edge is kept if it has direct,
non-orthology-transferred support from at least one of \code{experiments},
\code{database} (curated pathway/complex databases -- KEGG, Reactome,
MetaCyc, EBI Complex Portal, GO Complexes), \code{textmining}, or
\code{coexpression} -- STRING's genomic-context channels (neighborhood,
fusion, cooccurrence, homology) are not used as an inclusion criterion.
Kept edges are then filtered to STRING's overall \code{combined_score >= 500}
(0.5 on the 0-1 scale, i.e. roughly medium-to-high confidence). \code{cost} is
\code{max(0.01, 1 - combined_score)}, so lower cost = a stronger interaction.
}
\keyword{datasets}
