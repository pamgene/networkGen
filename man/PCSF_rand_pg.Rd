% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pcsf-core.R
\name{PCSF_rand_pg}
\alias{PCSF_rand_pg}
\title{Solve PCSF with randomized edge costs}
\usage{
PCSF_rand_pg(
  edges_df,
  terminals,
  n = 8,
  r = 0.1,
  w = 2,
  b = 1,
  mu = 5e-04,
  dummies = NULL
)
}
\arguments{
\item{edges_df}{Data frame with columns \code{head}, \code{tail}, \code{cost} (the PPI network).}

\item{terminals}{Named numeric vector: names are terminal node names
(matching \code{edges_df}), values are their prizes.}

\item{n}{Number of randomized runs to solve and combine into a consensus
network -- each run adds different random noise to edge costs (see \code{r}),
and the final result is the union across all \code{n} runs. Higher \code{n} gives
a more stable result at the cost of speed; the default (8) is often
lowered for faster iteration while developing/testing. Default 8.}

\item{r}{Fraction of random noise added to each edge cost per run (0-1),
used to build the \code{n}-run consensus (see \code{n}). Default 0.1.}

\item{w}{The fixed cost of connecting one terminal node to the network's
root -- effectively the "entry price" a terminal's prize (\code{b * prize})
must clear before it's worth including in the network at all. Trades off
directly against \code{b}: raising \code{w} (or lowering \code{b}) makes the algorithm
more willing to leave terminals out rather than pay to connect them.
Default 2.}

\item{b}{Multiplier applied to terminal-node prizes before they're weighed
against \code{w} (the cost of including a terminal at all). If no terminal's
\code{b * prize} exceeds \code{w}, PCSF finds no subnetwork worth building --
raise \code{b} (or lower \code{w}) if that happens. Default 1.}

\item{mu}{Hub-penalization strength: non-terminal (Steiner) nodes are
penalized by \verb{mu * their degree in the interactome}, discouraging the
algorithm from routing through very high-degree "hub" genes just because
they happen to connect to many things. Default 0.0005.}

\item{dummies}{Optional character vector of dummy-root target names;
defaults to \code{names(terminals)}.}
}
\value{
A list of class \code{c("PCSF_df", "list")} with \code{edges} (from/to/weight
data frame), \code{nodes} (name/prize/type data frame), and \code{n_runs}.
}
\description{
Runs the prize-collecting Steiner forest algorithm \code{n} times, each time
adding random noise to edge costs, and returns the union of all runs as a
consensus subnetwork. Dataframe-based -- skips \code{PCSF::construct_interactome()}.
A slower igraph-based implementation existed alongside this one and was
removed after confirming the two were bit-exact equivalent (including the
raw random values driving the solver) -- see
\verb{archive/pcsf-equivalence/} and \code{docs/adr/0005-remove-original-pcsf-path.md}.
}
