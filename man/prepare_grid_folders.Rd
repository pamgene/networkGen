% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grid.R
\name{prepare_grid_folders}
\alias{prepare_grid_folders}
\title{Create one output folder per distinct grid combination}
\usage{
prepare_grid_folders(
  grid,
  prepare_fn = prepare_run_params,
  base_labels = list(),
  ...
)
}
\arguments{
\item{grid}{Output of \code{\link[=build_network_grid]{build_network_grid()}}.}

\item{prepare_fn}{\code{\link[=prepare_run_params]{prepare_run_params()}}, or a package's own extension
of it (e.g. \code{networkScore::prepare_score_run_params()}, which adds
\code{nperms_network}/\code{relative_to} to the naming rule).}

\item{base_labels}{Named list of label overrides constant across every
combination (typically \code{uka}/\code{sens} -- see \code{\link[=capture_params]{capture_params()}}'s
\code{.labels}). A per-combination \code{ppi_network} label (from the grid
cell's own \code{ppi_network_name}) is added automatically; don't include
one here.}

\item{...}{Passed to \code{prepare_fn} for every combination (\code{respath},
\code{uka}, and whatever else \code{prepare_fn} needs) -- \code{spec_cutoff}/
\code{perc_cutoff}/\code{b}/\code{rank_uka_abs}/\code{ppi_network} come from each grid
cell itself, not from here.}
}
\value{
A list: \code{combo_key} (character vector, same length/order as
\code{grid}, tagging each cell with which combination it belongs to) and
\code{folders} (named character vector, the folder path for each distinct
\code{combo_key} value).
}
\description{
Shared by \code{\link[=run_network_grid]{run_network_grid()}} and \code{networkScore}'s scoring entry points
-- the one place "one \code{\link[=prepare_run_params]{prepare_run_params()}} folder per distinct
\verb{(spec_cutoff, perc_cutoff, b, w, rank_uka_abs, ppi_network)} combination,
given a grid" gets implemented, so it isn't duplicated between plain
generation and scoring.
}
