% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params.R
\name{prepare_run_params}
\alias{prepare_run_params}
\title{Capture a run's parameters, build its output folder, and write \code{params.csv}}
\usage{
prepare_run_params(..., .labels = list())
}
\arguments{
\item{...}{Passed to \code{\link[=capture_params]{capture_params()}}. Must include \code{respath} (the base
output directory this run's parameter-encoded folder is created under)
and whatever \code{\link[=build_param_folder]{build_param_folder()}} needs (see its docs).}

\item{.labels}{Passed to \code{\link[=capture_params]{capture_params()}} -- see its docs. Needed when
\emph{this} call is itself made by a relay (e.g. \code{run_network_grid()})
rather than directly by the end caller.}
}
\value{
The evaluated parameters as a named list (see \code{\link[=capture_params]{capture_params()}}),
with \code{respath} replaced by the created parameter-encoded folder path.
}
\description{
Composes \code{\link[=capture_params]{capture_params()}} and \code{\link[=build_param_folder]{build_param_folder()}}, then the
filesystem side effects: creates the resulting folder under \code{respath}
and writes \code{params.csv} there via \code{\link[=save_params]{save_params()}}. This is what real
callers use -- e.g. before \code{\link[=generate_kinase_network]{generate_kinase_network()}}/
\code{\link[=generate_paired_network]{generate_paired_network()}} with \code{write = TRUE}, passing this function's
resulting \code{respath} on as their \code{res.path}.
}
\details{
The following build call should read its arguments back off this
function's return value (e.g. \code{generate_kinase_network(spec_cutoff = run_params$spec_cutoff, ...)}) rather than retyping the same literals --
otherwise the two calls can drift apart, and \code{params.csv} would then
record different values than what was actually built.
}
