% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/augment.R
\name{augment_by_threshold_steps}
\alias{augment_by_threshold_steps}
\title{Augment a PCSF result with additional threshold-connected neighbors}
\usage{
augment_by_threshold_steps(
  pcsf_graph,
  full_graph,
  cost_threshold = 0.1,
  steps = 1
)
}
\arguments{
\item{pcsf_graph}{An \code{igraph} object -- the solved PCSF subgraph whose
node set is the starting point.}

\item{full_graph}{An \code{igraph} object -- the full PPI network to expand into,
with an edge attribute \code{cost}.}

\item{cost_threshold}{Maximum edge cost to follow when expanding. Default 0.1.}

\item{steps}{Number of expansion iterations. Default 1.}
}
\value{
The induced subgraph of \code{full_graph} on the expanded node set.
}
\description{
Starting from a solved PCSF subgraph's nodes, iteratively expands the node
set by following edges in \code{full_graph} whose cost is at or below
\code{cost_threshold}, for \code{steps} iterations, then returns the induced
subgraph of \code{full_graph} on the final node set.
}
\details{
Two bugs fixed while migrating this from \code{Network_generation}'s
\code{R/kinograte_PG.R} (unused there, so never actually exercised): the
starting node set referenced an undefined \code{core_nodes0} (typo for
\code{core_nodes}), and the loop reassigned \code{incident_edges} (a local variable)
over the function of the same name, which would break on any \code{steps > 1}
call.
}
