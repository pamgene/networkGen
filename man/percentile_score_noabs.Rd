% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/percentile-score.R
\name{percentile_score_noabs}
\alias{percentile_score_noabs}
\title{Percentile-rank a data frame by a metric, without taking the absolute value}
\usage{
percentile_score_noabs(df, symbol, metric, rank_lowest_highest = FALSE)
}
\arguments{
\item{df}{Data frame with the columns named by \code{symbol} and \code{metric}.}

\item{symbol}{Column (unquoted) with the row identifier.}

\item{metric}{Column (unquoted) to rank by.}

\item{rank_lowest_highest}{If \code{TRUE}, the lowest values of \code{metric} get the
highest \code{percentile_score} (e.g. most negative LogFC = most sensitive).}
}
\value{
\code{df}, deduplicated by \code{symbol} (renamed to \code{name}), with a
\code{percentile_score} column, sorted by that column descending.
}
\description{
Like \code{\link[=percentile_score]{percentile_score()}}, but ranks directly by \code{metric} (not its
absolute value) -- used where sign matters, e.g. sensitivity data where
"most sensitive" means most negative.
}
