% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pcsf-core.R
\name{call_sr}
\alias{call_sr}
\title{Call PCSF's compiled Steiner-forest solver}
\usage{
call_sr(from, to, cost, node_names, node_prizes)
}
\arguments{
\item{from, to, cost}{Edge list (character/character/numeric), including
\code{"DUMMY"} root edges.}

\item{node_names}{Character vector of all node names.}

\item{node_prizes}{Numeric vector of node prizes, same order as \code{node_names}.}
}
\value{
List with the solved edge list (as returned by the C++ solver).
}
\description{
Thin wrapper around the compiled \code{PCSF} package's \verb{_PCSF_call_sr} C++
entry point. Internal -- not exported.
}
\details{
\code{.Call(..., PACKAGE = "PCSF")} only works if \code{PCSF}'s namespace (and so
its compiled DLL) is already loaded -- unlike calling an R-level function
via \code{PCSF::something()}, it does \emph{not} trigger lazy loading itself. Listing
\code{PCSF} in \verb{Imports:} alone is not enough to guarantee that either: R only
eagerly loads an Imports package's namespace when this package's own
\code{NAMESPACE} has a real \code{import()}/\code{importFrom()} entry for it. The
\verb{@importFrom PCSF construct_interactome} below exists specifically to
create that entry, so \code{library(networkGen)} alone loads \code{PCSF} before any
\code{call_sr()} call can run -- this was missed originally (every test passed
under \code{pkgload::load_all()}, which eagerly loads all \verb{Imports:} regardless
of \code{NAMESPACE} directives, masking the gap) and only caught by testing a
real installed-and-\code{library()}d session.
}
\keyword{internal}
