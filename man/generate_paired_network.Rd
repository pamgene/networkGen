% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate.R
\name{generate_paired_network}
\alias{generate_paired_network}
\title{Generate a PCSF network from paired kinase-activity + sensitivity data}
\usage{
generate_paired_network(
  uka,
  sens,
  ppi_network = string_12.5,
  spec_cutoff,
  b = 2,
  w = 2,
  condition = NULL,
  res.path = NULL,
  art_nodes = NULL,
  art_lfc = NULL,
  write = FALSE,
  ...
)
}
\arguments{
\item{uka}{Pre-filtered kinase terminal-node data frame (columns \code{name},
\code{prize}, \code{type}, \code{LogFC}), e.g. from \code{\link[=uka_top]{uka_top()}}.}

\item{sens}{Pre-filtered sensitivity terminal-node data frame, same shape,
e.g. from \code{\link[=sens_top]{sens_top()}}.}

\item{ppi_network}{Data frame with columns \code{head}, \code{tail}, \code{cost} -- the
PPI network to search over. Defaults to the bundled
\link{string_12.5} reference network (lazy-loaded from
the package's own data, not a global variable -- pass a different data
frame explicitly, e.g. \link{ppi_networkv12_502_kins}, to use another one).}

\item{spec_cutoff}{Specificity cutoff, recorded in \code{maintitle}/output file
names.}

\item{b, w}{The two main PCSF cost knobs -- \code{b} multiplies node prizes
(higher \code{b} = bigger network), \code{w} is the cost of connecting a terminal
straight to the root (higher \code{w} = isolated terminals dropped). See
\code{\link[=PCSF_rand_pg]{PCSF_rand_pg()}} for the full picture. Both default 2.}

\item{condition}{Condition/comparison label, recorded in \code{maintitle}/output
file names.}

\item{res.path}{Output folder, used only when \code{write = TRUE}.}

\item{art_nodes, art_lfc}{Optional artificial terminal nodes to add before
building: \code{art_nodes} is a character vector of names, \code{art_lfc} their
\code{LogFC} values (prize fixed at 1, type \code{"Artificial"}).}

\item{write}{If \code{TRUE}, write \code{nodes_*.csv}/\code{missing_nodes_*.csv} to
\code{res.path}. Default \code{FALSE}.}

\item{...}{Passed through to \code{\link[=PCSF_rand_pg]{PCSF_rand_pg()}} (\code{n}, \code{r}, \code{mu}, \code{seed}).}
}
\value{
A \code{"networkGen_result"} object (see \code{\link[=new_network_result]{new_network_result()}}), or
\code{NULL} if PCSF could not find a subnetwork for these inputs.
}
\description{
The pure generation step for the "golden" (paired) analysis: combines
pre-filtered kinase and sensitivity terminal-node data frames (e.g. from
\code{\link[=uka_top]{uka_top()}} / \code{\link[=sens_top]{sens_top()}}) and builds one PCSF network. Computes no
topology statistics (that's \code{networkScore}'s job) and does no pathway
enrichment or plotting (that's \code{networkPlot}'s job).
}
