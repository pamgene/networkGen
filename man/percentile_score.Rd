% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/percentile-score.R
\name{percentile_score}
\alias{percentile_score}
\title{Percentile-rank a data frame by a metric}
\usage{
percentile_score(df, symbol, metric, desc = FALSE)
}
\arguments{
\item{df}{Data frame with the columns named by \code{symbol} and \code{metric}.}

\item{symbol}{Column (unquoted) with the row identifier (e.g. protein name).}

\item{metric}{Column (unquoted) to rank by.}

\item{desc}{If \code{TRUE}, rank descending by \code{-abs(metric)} instead of \code{abs(metric)}.}
}
\value{
\code{df}, deduplicated by \code{symbol} (renamed to \code{name}), with a
\code{percentile_score} column, sorted by that column descending.
}
\description{
Ranks rows by the absolute value of \code{metric}, producing a \code{percentile_score}
column in [0, 1] (1 = strongest). Copied in from the (now unused,
dormant since 2022) \code{kinograte} package's \code{percentile_rank()} -- see
\code{vignette("networkGen")} / project ADRs for why this isn't a live
dependency. Renamed from \code{score} to \code{percentile_score} to avoid colliding
with networkScore's unrelated "significance score" concept.
}
