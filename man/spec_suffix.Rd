% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{spec_suffix}
\alias{spec_suffix}
\title{Build the \verb{_spec<cutoff>} filename suffix, omitted when there's no filter}
\usage{
spec_suffix(spec_cutoff)
}
\arguments{
\item{spec_cutoff}{The specificity cutoff used for this run.

Vectorized: safe to use on a single value or a vector of them.}
}
\value{
\code{""} where \code{spec_cutoff} is \code{0}; \code{"_spec<spec_cutoff>"} elsewhere.
}
\description{
\code{spec_cutoff = 0} means "no specificity filtering" -- encoding it in every
output filename as \verb{_spec0} would just be noise, so it's dropped entirely
in that case.
}
\keyword{internal}
